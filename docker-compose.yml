version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=your_mongodb_connection_string_here
    depends_on:
      - nlp

  nlp:
    build: ./service
    ports:
      - "8000:8000"
